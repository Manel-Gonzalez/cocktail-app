/* import { API_URL, API_SEARCH } from "./settings"



export default async function getCocktailsByBrowser(keyword = "old") {
  const apiURL = `${API_URL}/${API_SEARCH}${keyword}`
  const res = await fetch(apiURL)
  const drinks = await res.json()

  return drinks
}

 */
  //then/catch
/*   return fetch(apiURL)
    .then((res) => res.json())
    .then(console.log("drinks", drinks)) */